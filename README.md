# Tomato Classification (Reject / Ripe / Unripe)

## Structure
- `src/pcd.ipynb` : training notebook
- `src/test_model.py` : run single-image inference
- `Three_Classes/` : dataset folder (not included in repo)

Expected dataset structure:
Three_Classes/
  Reject/
  Ripe/
  Unripe/

## Setup
```bash
pip install -r requirements.txt
